import React from 'react';

const AccountantDashboard = () => {
    return (
        <div>
            <h2>Accountant Dashboard</h2>
            {/* Add functionality to interact with billing and payroll endpoints */}
        </div>
    );
};

export default AccountantDashboard;
